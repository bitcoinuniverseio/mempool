[package]
name = "universe-workbench-engine"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
miniscript = { version = "=12.3.7", features = ["compiler"] }
serde_json = "=1.0.145"

[profile.release]
panic = "abort"
