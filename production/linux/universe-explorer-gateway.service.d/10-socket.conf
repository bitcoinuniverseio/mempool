# Binds the gateway service to the socket unit that owns its port.
#
# Requires plus After means starting the service brings the socket up first,
# and, more to the point, restarting the service leaves the socket up. That is
# the whole reason this exists: the port survives the process.

[Unit]
Requires=universe-explorer-gateway.socket
After=universe-explorer-gateway.socket
