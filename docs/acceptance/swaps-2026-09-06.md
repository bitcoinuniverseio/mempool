# Swap continuation, 2026-09-06

**FUNCTIONAL NO-GO. No real-network swap pass.** This scoped continuation uses source baseline `b8d5e3bdc837681c7ce2b8cb91616262f432a313`. The [previous evidence](swaps-2026-09-05.md) remains historical. Current changes preserve the Bitcoin two-leaf evidence reader, script verification, observation store and unsigned PSBT generator.

## Current repair and PRE-04

The scoped service/routes, `backend/src/config.ts`, sample configuration and candidate environment variable names contain no configured authenticated provider registry, accepted manifest trust loader or settlement receipt source. The candidate backend directory has sample/test configuration, but no local `mempool-config.json`. No credential values were needed for this check. The configured swap observation implementation reads/writes `swap_lockup_observation` in the existing `intelligence_events` lifecycle; it does not contain authenticated provider health records.

Missing integration code and missing external evidence remain separate blockers:

- D-SWAP-03 remains incomplete: provider registry loading, health ingestion/history and manifest verification have no actual owned contract to implement. PRE-04 must supply the owned provider identity/trust source, exact signed representation and signing scheme, exact normalized bytes, provider/domain/network binding, expiry, revision and rotation rules, plus the authenticated health source and source-time semantics. PRE-02 is also required for real durable observation readback.
- D-SWAP-04 remains incomplete: the intended server-side authenticated receipt source, receipt schema and provider/swap/network/payment/asset/amount commitments, settlement states, replay/idempotency rules and reorg handling are unavailable. `reconcileCrossLayer()` still returns `insufficient_private_evidence`. No caller key, claimed status or public script match substitutes for these facts.

The controllable failure contract is repaired. `/providers`, `/providers/:providerId` and `/providers/:providerId/history` now return HTTP 503 with `stage: unavailable-registry`, rather than a successful empty directory or an unknown-provider claim. The detail 404 branch is explicitly `unknown-provider` and is reserved for an actual completed registry lookup. No such authenticated lookup is available in this candidate.

`/manifests/verify` returns HTTP 503 with `valid: false` and `stage: unavailable-registry` for object submissions. Non-object or oversized request envelopes remain HTTP 400. The previous assumed compressed secp256k1 key and fixed 64-byte signature checks were removed: an absent owned contract cannot establish a signature encoding, algorithm or trust root.

Provider directory/detail consumers show these errors. On route/network changes they clear previous records, cancel old requests and continue after errors. An absent route ID is an input error; it no longer substitutes `boltz-exchange`. No fake successful provider, history or manifest UI has been added.

## Current local checks

Executed on 2026-09-06 around 03:00 UTC. Controlled transaction fixtures remain synthetic unit fixtures and are not Signet evidence.

| Evidence ID | Command or check | Result |
| --- | --- | --- |
| SWR-010 | Backend `node node_modules/jest/bin/jest.js src/api/intelligence/swaps/swaps.test.ts --runInBand --coverage=false` | 48 passed. Existing two-leaf lockup/spend/recovery/signing tests plus 503 registry, reserved 404, malformed manifest and self-declared-key regressions. |
| SWR-011 | Frontend `node node_modules/vitest/vitest.mjs run src/app/universe/swaps/swaps-package.spec.ts src/app/universe/swaps/swaps-simulation.spec.ts` | 36 passed. Independent artifact decode, real inspector/recovery/provider handlers, route/network cancellation and error recovery, and local simulation. Shared visual dependencies are isolated for handler tests; this is not browser evidence. |
| SWR-012 | Backend `node node_modules/typescript/bin/tsc -p tsconfig.build.json --noEmit`; frontend `node node_modules/typescript/bin/tsc -p tsconfig.app.json --noEmit` | Both passed. |
| SWR-013 | Backend `node node_modules/eslint/bin/eslint.js src/api/intelligence/swaps --ext .ts --quiet`; scoped `git diff --check` | Both passed. |

The integration owner owns combined candidate builds, gateway/runtime evidence and the single localhost browser tab. This worker launched no service, browser, CI, runner, deployment or transaction, and made no commit.

## Offered operation statuses

| ID | Current acceptance | Remaining dependency |
| --- | --- | --- |
| SW-01 | BLOCKED; unknown totals retained | PRE-02 durable actual observation readback and PRE-01 real checkpoint/reorg evidence. |
| SW-02 | Catalog checks retained; authenticated provider revision/support BLOCKED | PRE-04 provider provenance and PRE-03 actual network support. Catalog declarations are not support acceptance. |
| SW-03, SW-04, SW-05, SW-06 | BLOCKED; local unavailable/unknown error distinction repaired | PRE-04 registry, real health history and accepted signed-manifest contract. SW-05 also requires permitted durable storage. |
| SW-07, SW-08, SW-09 | BLOCKED; existing local lockup/claim/refund verification regression passes | PRE-01 owned supported node and actual controlled lockup or confirmed claim/refund with public commitments. |
| SW-10, SW-12 | BLOCKED; local unsigned generator and independent decoder regressions pass | PRE-01 real controlled lockup, selected authoritative checkpoint and compatible controlled signer; actual UI copy/download/reload proof belongs to the integration owner. Refund broadcast is not required for these rows. |
| SW-11 | BLOCKED; truthful `insufficient_private_evidence` retained | PRE-04 authenticated specific settlement receipt plus independent chain evidence. |
| SW-UI-INSPECT | PASS, LOCAL HANDLER for public JSON validation; chain action BLOCKED | SWR-011 proves local operation, private-field rejection and request cancellation. Actual chain action needs SW-07. Browser evidence remains separate. |
| SW-UI-SIM | PASS, LOCAL LOGIC/HANDLER | SWR-011. No actual reorg, eviction or recovery is inferred. |

## Explicit adapter decisions

These decisions describe the existing offering, not a new protocol exclusion. The catalog rows and their disclosure checks remain in scope.

| Entry | Existing selector/route/source evidence | Execution decision |
| --- | --- | --- |
| `boltz_submarine_v2`, submarine/reverse Bitcoin two-leaf | `/swaps/inspect`, `/swaps/recover`, `/public-receipts/verify`, `/chain-context`; `boltzContract()` explicitly selects this protocol and these two swap types | Offered. Real-network verification/generation remains BLOCKED under SW-07 through SW-12. |
| `boltz_chain_v1` | `listProtocols()` catalog and `/swaps/chain` explanatory table; no execution form/select binding for this adapter. `boltzContract()` rejects its ID with `unsupported-adapter`. | Execution N/A for the existing reference-only catalog. Catalog provenance/disclosure checks retained. |
| `lightning_loop_v1` | `listProtocols()` catalog only; no Loop-specific execution selector or route. `boltzContract()` rejects its ID. | Execution N/A for the existing reference-only catalog. Catalog checks retained. |
| `ark_vhtlc_v1` | `listProtocols()` and `/swaps/chain` Ark reference row with proof adapter unavailable; no VHTLC execution selector. `boltzContract()` rejects its ID. | Execution N/A for the existing reference-only catalog. Catalog checks retained. |
| Liquid confidential value/PSET | `/swaps/chain` reference table and catalog network declarations only. Public request context and recovery network selector are Bitcoin-only. No PSET generator or execution form exists. | Execution N/A for the reference entry; no Signet support or Bitcoin-to-Liquid proof inference. |
| Cooperative/key-path | Package model and provider declaration fields only. No cooperative/key-path execution selector; implemented spend proof requires the supported script path. | Execution N/A for these declarations; declaration checks retained. |
| Lightning signed settlement | `/chain-context` explicitly returns `reconciliation`; `/swaps/submarine` describes settlement verification. Existing offchain proof is unavailable. | Offered reconciliation remains BLOCKED under SW-11/PRE-04. It is not converted to N/A. |
